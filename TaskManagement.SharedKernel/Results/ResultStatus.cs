namespace TaskManagement.SharedKernel.Results
{
    public enum ResultStatus
    {
        Ok = 0,
        NotFound,
        Invalid,
        Error,
        Forbidden,
        Unauthorize,
        Conflict
    }
}
